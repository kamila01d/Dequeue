# Dequeue

Dequeue implementation in C++ with its basic methods. 

To run program using makefile type:
               ``` make run ```
To clean the folder from executables:
                make clean
